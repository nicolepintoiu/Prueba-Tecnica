import { ContactsPage } from "./features/contacts/ContactsPage";

export default function App() { //el componente app es el principal del archivo y es el que se montas desde la pantalla desde main
  return <ContactsPage />;
}

